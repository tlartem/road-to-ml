# Kubernetes secrets (contain credentials, plain-text)
**/secret.yaml
mlops-platform/argocd/repo-secret.yaml

# Sealed secrets are safe to commit (encrypted)
!**/sealed-secret.yaml
!**/sealed-repo-secret.yaml

# Gitea data (docker-compose volumes)
gitea-data/
act-runner-data/

# Environment variables (runner tokens, etc.)
.env

# Python
__pycache__/
*.pyc
.venv/
